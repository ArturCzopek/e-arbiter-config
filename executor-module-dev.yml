e-arbiter:
  swagger:
    enabled: true
    title: e-Arbiter - Executor Module
    description: API for Executor Module

spring:
  profiles: dev

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}